import { Text } from "react-native";

export default function Home() {
    return (
        <Text>This is the home screen.</Text>
        // Possible options for content on this screen
        // Todays meal plan (summary)
        // Todays calorie summary
    )
}